# NPM Configuration pour EshopAng
# Configuration pour gérer les dépendances avec compatibilité

# Permettre les dépendances peers optionnelles non strictes
legacy-peer-deps=true

# Authentifier auprès de registres si nécessaire
# registry=https://registry.npmjs.org/

# Paramètres de sécurité
audit=true
audit-level=moderate

# Audit CI
ci=false
